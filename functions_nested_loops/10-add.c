#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: First integer
 * @b: Second integer
 *
 * Return: the add of two integers
 */
int add(int a, int b)
	{
	return (a + b);
	}
