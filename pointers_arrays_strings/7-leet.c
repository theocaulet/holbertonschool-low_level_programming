#include "main.h"
#include <stdio.h>

/**
 * *leet -  encodes a string
 * Return: always 0
 */
char *leet(char *)
{
	int i, j;
	char base;
	char leet;

	for (i = 0; str[i] != '\0'; i++)
	{
	for (j = 0; base[j] != '\0'; j++)
	{
	if (str[i]; == base[j])
	{
	str[i] = leet[j]
	}
	}
	}
	return (0);
}
